interface Window {
    REACT_APP: {
        CREQ: string;
        NODE_ENV: string;
        DEV_SERVER_PORT: number;
        API_URL: string;
        DEFAULT_LANGUAGE: string;
        ACS_TRANSACTION_ID: string;
        ERROR: string;
        ERROR_POP_UP_TIMEOUT_IN_MS: number;
        AUTHORIZATION_CODE_FROM_CIAM: string;
    };
}
